import 'package:flutter/material.dart';

class Countrystartscreen extends StatelessWidget {
  const Countrystartscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
